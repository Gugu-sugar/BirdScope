# docker-compose 默认已内置这些值，通常不需要单独建 .env。
# 如需自定义端口或密码，可复制本文件为 .env 并修改，compose 会自动读取。

# 数据库（容器内 api 通过服务名 db 连接，宿主机调试可用 localhost:5432）
POSTGRES_DB=birdscope
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# 对外暴露端口（若 5432 / 8000 被占用，改这里）
# DB_PORT=5432
# API_PORT=8000
