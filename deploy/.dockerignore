# 构建上下文是仓库根，排除运行时不需要 / 过大 / 敏感的内容
**/__pycache__/
**/*.pyc
**/.pytest_cache/

# 大体量原始数据与降采样产物（运行时 API 不需要）
backend/data/
backend/test_data/

# 敏感与本地配置
backend/.env
**/.env

# 数据库 dump（单独传输，不进镜像）
deploy/dump/*.dump

# git 与文档
.git/
docs/
